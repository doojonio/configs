startup_message off
hardstatus alwayslastline
hardstatus string '%{= kG}[ %{G}%H %{g}][%= %{= kw}%?%-Lw%?%{r}(%{W}%n*%f%t%?(%u)%?%{r})%{w}%?%+Lw%?%?%= %{g}][%{B} %d/%m %{W}%c %{g}]'
altscreen on
defbce "on"

bind j focus down
bind k focus up
bind l focus right
bind h focus left

rendition so kb

screen -t ranger
screen -t output
screen -t something

select 0
split
focus
select 2
resize -v 20%
split -v
focus
select 1
focus
